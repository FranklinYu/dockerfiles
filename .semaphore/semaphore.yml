version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: build
    task:
      jobs:
        - name: Docker build
          commands:
            - checkout
            - docker build --tag=franklinyu/sinatra-hello sinatra-hello
